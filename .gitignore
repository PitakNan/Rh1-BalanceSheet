master.parquet
*.pyc
__pycache__/
mapping_review_for_accounting.csv

# ── ไฟล์ที่เกิดระหว่างทำงาน ไม่ต้องเข้า repo (18 ส.ค. 69) ──
# ⚠️ 2 บรรทัดแรกสำคัญที่สุด: docs/ คือตัวเว็บที่เผยแพร่จริง — ไฟล์อะไรที่หลุดเข้าไปจะขึ้นเว็บสาธารณะ
#    ไฟล์สำรองพวกนี้ค้างอยู่ในเครื่องตั้งแต่ มิ.ย. 69 กัน `git add .` เผลอดันขึ้นเว็บ
docs/*backup*.html
docs/*.backup-*.html
# สคริปต์รุ่นเก่าที่สำรองไว้ตอนแก้ใหญ่ (ตัวจริงยัง track อยู่ปกติ)
pipeline/*_backup_*.py
# ผลลัพธ์ที่สคริปต์สร้างเอง — สร้างใหม่ได้เสมอจากการรันสคริปต์
pipeline/out/
pipeline/logs/
pipeline/*.log
