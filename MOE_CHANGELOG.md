# บันทึกการเปลี่ยนแปลงโมเดล MOE — แท็บ #exec (risk_drill.html)

ไฟล์นี้เก็บ "ประวัติการเปลี่ยนแปลง" แบบเรียงตามเวลา (ต่างจาก `RISK_EXEC_MODEL.md` ที่เป็นคู่มือ
อธิบายสูตร/กฎที่ใช้อยู่ ณ ปัจจุบัน) — ใช้เป็นวัตถุดิบตอนทำคู่มือสรุปการเปลี่ยนแปลงฉบับเต็ม
ตั้งแต่เวอร์ชันก่อนหน้าจนถึงจบรอบปรับปรุงทั้งหมด **บันทึกต่อได้เรื่อย ๆ ทุกครั้งที่มีการเปลี่ยนแปลงรอบใหม่**

---

## Baseline — ก่อนรอบปรับปรุงนี้ (ถึง commit `bb8c70a`)

ตาราง "ผลจำลอง + เงินสนับสนุนที่ต้องใช้" ในแท็บ #exec ใช้ **นิยาม MOE เดียวตายตัว**:

- ชุดบัญชี MOE = **62 บัญชี** ("MOE.Ver68" ในเอกสารรอบนี้) ตามที่กองเศรษฐกิจสุขภาพกำหนด 14 ก.ค. 69
- ไม่มีตัวเลือกให้สลับนิยาม ไม่มีทางเลือกเรื่องจ่ายเจ้าหนี้การค้าบางส่วน
- เงินสำรอง MOE ทั้งเขต = 1,560.46 ลบ. · แห่งที่ควักเงินทำอย่างอื่นไม่ได้ = 4 แห่ง
  (ทุ่งหัวช้าง เทิง ขุนตาล เวียงเชียงรุ้ง)
- เงินสนับสนุนเป้าระดับ 6 = 21.05 ลบ. · 6 แห่ง

---

## รอบปรับปรุง 1 — เพิ่มตัวเลือกนิยาม MOE 3 แบบ (6 ส.ค. 69 · commit `336a1c8`)

### โจทย์ที่เจ้าของงานให้มา
เดิม MOE/เดือน คำนวณจาก 62 บัญชี ("MOE.Ver68") — ต้องการเพิ่ม:
1. **MOE.Ver69** — ชุดบัญชีใหม่ 59 รายการที่ระบุมา (แทนที่ Ver68 เป็นค่าเริ่มต้น)
2. **MOE.Ver69 + จ่ายหนี้การค้าบางส่วน X%** — เมื่อเลือก ให้ใช้ Ver69 แต่จ่ายเงินสดจริงเฉพาะ
   9 บัญชี (ยา/เวชภัณฑ์ + ครุภัณฑ์มูลค่าต่ำกว่าเกณฑ์) ตามสัดส่วน % ที่กรอก ที่เหลือค้างเป็นเจ้าหนี้การค้า

### คำถามที่ถามกลับ + คำตอบที่ได้ (ก่อนเริ่มโค้ด)
| คำถาม | คำตอบเจ้าของงาน |
|---|---|
| ส่วนที่เลือกไม่จ่าย ให้กระทบบัญชียังไง (แบบ A ตัดหนี้ตรงบัญชี vs แบบ B ลดแค่ตัวเลขเงินสด) | **แบบ A** — ค้างเป็นเจ้าหนี้การค้าจริง ไม่ใช่แค่ปรับตัวเลข |
| ให้เวอร์ชันไหนเป็นค่าเริ่มต้น | **Ver69** |
| ค่าเช่าเบ็ดเตล็ด (บัญชีใหม่ที่มีเฉพาะ Ver69) จัดกลุ่มไหน | ให้ไปอยู่กลุ่ม **"ค่าเช่า"** (กลุ่มใหม่แยกจากกลุ่มเดิม) |

### สิ่งที่พบระหว่างทำ (ของแถมที่ไม่ได้ถามแต่ตรวจแล้วสำคัญ)
- ของเดิม "62 บัญชี" มีจริง **59 บัญชีที่มีผล** — อีก 3 บัญชี (`5104040199.106/.107/.108`
  ค่าตอบแทนไม่ทำเวชปฏิบัติ ชุดรหัสเดิม) ไม่อยู่ในชุดค่าใช้จ่าย `3010X` ตามผังบัญชี → ยอดเป็น 0 มาตลอด
  ดังนั้นผลต่างจริงระหว่าง Ver68/Ver69 มีแค่ **2 บัญชี**:
  - Ver68 มี Ver69 ไม่มี: `5101010113.103` ค่าจ้างชั่วคราว(บริการ) = 39.13 ลบ./ด.
  - Ver69 มี Ver68 ไม่มี: `5104030212.101` ค่าเช่าเบ็ดเตล็ด = 56.83 ลบ./ด.
- `export_exec.py` เพิ่มการเตือนอัตโนมัติเมื่อพบบัญชี MOE ที่หลุดจากชุด `3010X` (กันเงียบซ้ำในอนาคต)

### ตัวเลขที่เปลี่ยน (รอบที่ 1)
| | Ver68 (เดิม) | Ver69 (ใหม่ — ค่าเริ่มต้น) | Ver69 + จ่าย 50% |
|---|---|---|---|
| MOE ทั้งเขต/เดือน | 1,391.42 ลบ. | **1,409.12 ลบ.** | 1,022.04 ลบ. |
| เงินสนับสนุนเป้า 6 | 21.05 ลบ. · 6 แห่ง | 21.05 ลบ. · 6 แห่ง (ไม่ขยับ — ดูรอบ 3) | เท่ากัน |
| ส่วนขาดสภาพคล่อง 3 ด. | 369.75 ลบ. | 375.10 ลบ. | 171.63 ลบ. |

---

## รอบปรับปรุง 2 — ค่าเช่าค้างไม่ได้ (6 ส.ค. 69 · รวมใน commit `336a1c8`)

### สิ่งที่เกิดขึ้น
ตอนแรกตั้งกลุ่ม "ค่าเช่า" เป็น **ค้างเป็นเจ้าหนี้การค้าได้** (ไม่เข้าฐานเงินสำรอง MOE) โดยเดา
เจ้าของงานยืนยันกลับมา: **"ค่าเช่าค้างไม่ได้"** → ต้องเป็นกลุ่มที่ **ต้องชำระตามกำหนด** เหมือน
ค่าจ้าง/ค่าตอบแทน/สาธารณูปโภค

### ผลที่ตามมา
- ธง `cash` ของกลุ่ม `rent` เปลี่ยนจาก `false` → `true` ใน `MOE_CASH_G`
- เงินสำรอง MOE ทั้งเขต: 1,443.07 ลบ. (ตัวเลขชั่วคราวตอนยังไม่แก้) → **1,613.56 ลบ.**
- แห่งที่ควักเงินทำอย่างอื่นไม่ได้: กลับมาเป็น **4 แห่ง** เท่าของเดิม (ทุ่งหัวช้าง เทิง ขุนตาล เวียงเชียงรุ้ง)

### ศัพท์ที่แก้ไปพร้อมกัน (เจ้าของงานถามว่า "ยืดได้" คืออะไร — ไม่ใช่ศัพท์การเงินปกติ)
เลิกใช้คำที่ผู้ช่วยตั้งเอง เปลี่ยนเป็นศัพท์มาตรฐาน 20 จุดในหน้าเว็บ:

| เดิม (คำที่ตั้งเอง) | ใหม่ (ศัพท์มาตรฐาน) |
|---|---|
| "ยืดไม่ได้" | ค่าใช้จ่ายที่ต้องชำระตามกำหนด (ไม่มีเครดิตเทอม) |
| "ยืดได้" | ค้างเป็นเจ้าหนี้การค้าได้ (ผู้ขายให้เครดิตเทอม) |
| "ยืดจ่าย" (แผง Survival Simulator เดิม) | ค้างชำระนานขึ้น |

---

## คำถาม/ข้อสงสัยที่เจ้าของงานถามหลัง deploy (7 ส.ค. 69) — ไม่มีการแก้โค้ด เป็นการอธิบาย

### Q1: ทำไมเลือกจ่ายหนี้การค้าน้อยลงแล้ว "เงินสนับสนุนที่ต้องใช้" ไม่ลดลงตาม
**คำตอบ:** ตารางมี 2 ตัวเลขคนละความหมาย
- **"ส่วนขาดสภาพคล่อง"** (stress test) → ลดลงตามสัญชาตญาณจริง (9.38 → 5.89 ลบ. ที่แม่วาง เมื่อจ่าย 100%→0%)
- **"เงินสนับสนุนที่ต้องใช้"** (Solver หาเงินให้ถึงเป้าระดับ 6) → **ไม่ขยับเลย** (9.15 ลบ. คงที่ทุก %)
  เพราะ 6 แห่งที่ต้องเติมเงินติดเกณฑ์ **NWC ติดลบ + NI ติดลบ** ไม่ใช่เกณฑ์เงินสด — และการค้างหนี้การค้า
  ทำให้สินทรัพย์หมุนเวียนกับหนี้สินหมุนเวียนเพิ่มขึ้น**เท่ากันทุกเดือน** (NWC = สินทรัพย์ − หนี้สิน จึงหักกันหมด
  ไม่ขยับ) ส่วน NI ไม่ขยับเพราะค่าใช้จ่ายรับรู้ตามเกณฑ์คงค้างแล้ว ไม่ว่าจะจ่ายเงินสดหรือค้างไว้

### Q2: ตัวเลือก dropdown ทำงานถูกไหม (เปลี่ยนแล้ว MOE/เดือนต้องเปลี่ยนตาม)
**ยืนยันแล้ว** — จำลองคลิกจริงตามลำดับ (เปิดหน้า → เลือก dropdown → ปรับ % ) ยืนยันว่า MOE/เดือน
ลดลงตามจริง: 3.1 ลบ. (Ver69) → 2.5 ลบ. (+จ่าย 50%) → 2.1 ลบ. (+จ่าย 20%) ที่แม่วาง — ทำงานถูกต้อง
ตามที่ต้องการ ไม่มีบั๊ก

---

---

## รอบปรับปรุง 3 — เปิดคอลัมน์ลูกหนี้ + นับเป็นเงินเข้า + รพช. hub (6 ส.ค. 69)

### โจทย์ที่เจ้าของงานให้มา (3 ข้อ ทำต่อเนื่องกันในวันเดียว)
1. **เปิดคอลัมน์ "ลูกหนี้ค่ารักษา UC-OP นอก CUP ในจังหวัด"** ในตารางผลจำลอง (ต่อจากคอลัมน์เจ้าหนี้)
2. **ให้ลูกหนี้กลายเป็นเงินที่เข้ามา** บวกเพิ่มในคอลัมน์ "เงินสดคงเหลือหลังภาระ MOE ถึง ก.ย.69"
   เพราะจะได้รับชำระจากกลุ่มที่ต้องจ่ายเจ้าหนี้ + ให้มียอดรวมลูกหนี้ในแถวชิปสรุปข้างหัวตารางด้วย
3. **เพิ่ม รพช. ที่เป็น Node เข้าตาราง Option พิเศษ** (แม่สาย สันป่าตอง เกาะคา ปัว) — และถามว่า
   ทำปุ่มเพิ่มชั่วคราวที่หายหลังรีเฟรชได้ไหม → **ทำได้ ทำให้แล้ว**

### สิ่งที่พบระหว่างทำ (ของแถมที่ไม่ได้ถามแต่ตรวจแล้วสำคัญ)
- **ข้อ 1 ไม่ต้องเขียนใหม่เลย** — คอลัมน์นี้ถูก "ซ่อน" ไว้ตั้งแต่ 5 ส.ค. 69 (`EX_SHOW_TJAR=false`)
  ไม่ได้ลบ · สูตร/ตัวเรียง/tooltip/TSV ยังอยู่ครบ แค่พลิกธงกลับเป็น `true`
- **ชุดตรวจ 2 ไฟล์ฝัง "หัวตาราง 14 ช่อง" ไว้ตายตัว** (สมมติว่าคอลัมน์นี้ซ่อนอยู่ตลอดไป) → แก้ให้
  อ่านค่า `EX_SHOW_TJAR` แล้วคิดจำนวนคอลัมน์เอง สลับเปิด/ปิดอนาคตชุดตรวจไม่พังอีก
- **🪤 บั๊กที่ตามมาจากข้อ 3 (จับได้เอง ไม่ได้ถูกรายงาน)** — เดิมฝั่งลูกหนี้ (รพช.) กับฝั่งผู้รับภาระ
  (รพศ./รพท.) เป็นคนละ type เสมอ โค้ดคอลัมน์ "เงินที่ยกให้" จึงเช็คบทบาทแรกแล้ว return ทันที
  แต่ **รพช. hub เป็นได้ทั้งสองบทบาทพร้อมกัน** (ค้างหนี้ตัวเองเล็ก + รับภาระจาก รพช. เล็กรอบข้างก้อนใหญ่)
  → ต้องหักลบเป็นค่าสุทธิ เช่น เกาะคา ได้รับยกหนี้ 4.74 ลบ. แต่ต้องยกให้ 6.57 ลบ. = **ยกให้สุทธิ 1.82 ลบ.**
  (ก่อนแก้จะโชว์ "ได้รับ 4.74 ลบ." ซึ่งกลับข้างความจริง) · ตัวจำลองเงินสด/NI จริงไม่ต้องแก้ เพราะเขียน
  เป็น 2 บล็อกอิสระอยู่แล้ว — บั๊กอยู่ที่ตัวแสดงผลเท่านั้น
- **การเพิ่ม hub ช่วยลดหนี้ที่เกลี่ยไม่ได้จริง** — `uncovered` ลดจาก 14.4 → 7.8 ลบ.

### ตัวเลขที่เปลี่ยน (รอบที่ 3)
| | ก่อน | หลัง |
|---|---|---|
| คอลัมน์ในตาราง | 14 | **15** (เพิ่มลูกหนี้) |
| สูตรเงินสดคงเหลือหลังภาระ MOE | (เงินสด − เจ้าหนี้) − MOE×เดือน | **(เงินสด − เจ้าหนี้ + ลูกหนี้) − MOE×เดือน** |
| ชุดผู้รับภาระใน Option พิเศษ | 12 (รพศ.3+รพท.9) | **16** (+ hub 4) |
| หนี้ที่เกลี่ยไม่ได้ (uncovered) | 14.4 ลบ. | **7.8 ลบ.** |
| เงินสนับสนุนรวมเป้า 6 | 21.05 ลบ. · 6 แห่ง | **ไม่ขยับ** (คนละสูตร — เหตุผลเดียวกับ Q1 ข้างบน) |

⚠️ **สมมติฐานที่ต้องรู้**: คอลัมน์นี้สมมติว่า**เก็บลูกหนี้ได้เต็มจำนวน** ไม่ได้หักส่วนที่ Option ตามจ่าย
บอกว่าเกลี่ยไม่ได้ — ตั้งใจไม่ผูกกัน เพราะเป็น stress test แยกอิสระจาก Option (คนละ toggle)

---

## รอบปรับปรุง 4 — คำเตือนส่วนต่างลูกหนี้ > เจ้าหนี้ (6 ส.ค. 69 · ต่อเนื่องรอบ 3)

### สิ่งที่เจ้าของงานสังเกตเอง
เห็นตัวเลขที่รายงานกลับไปแล้วตั้งข้อสังเกตว่า **เจ้าหนี้รวม 309.9 ลบ. (103 แห่ง) น้อยกว่า
ลูกหนี้รวม 499.3 ลบ. (101 แห่ง)** → "แสดงว่าลูกหนี้อาจจะมาจาก รพ.รัฐสังกัดกรมอื่น" สั่งให้ใส่ note/popup เตือนไว้

### สิ่งที่แก้
เดิมคู่มือ (7.7) อธิบายส่วนต่างนี้ด้วยเหตุ **"บันทึกแยกเล่ม" อย่างเดียว ซึ่งอธิบายไม่ครบ** —
ส่วนต่าง **189.4 ลบ.** ยังชี้ว่าคู่หนี้บางส่วนอยู่นอกชุด 103 แห่ง (สังกัดกรมการแพทย์/กรมสุขภาพจิต ·
รพ.นอกเขต · เอกชนคู่สัญญา) ซึ่ง Option ตามจ่ายเกลี่ยให้ไม่ได้ ต้องตามเก็บจากหน่วยต้นทางจริง

ใส่คำเตือน 4 จุด: การ์ด 📋 สรุประดับเขต (บรรทัดที่เห็นได้เลยไม่ต้อง hover) · แผง Option พิเศษ ·
tooltip คอลัมน์ลูกหนี้ · tooltip ชิป 📥 — ทุกจุดกำกับไว้ว่า **"ยังไม่ได้ยืนยันคู่หนี้รายตัวกับ
ตารางเคลียร์ริ่งของ สสจ./เขต"** (เป็นข้อสังเกตจากยอดบัญชี ไม่ฟันธง)

### 🪤 พลาดเองแล้วแก้ทันในรอบเดียวกัน
วางคำเตือนในแผง Option ไว้ในกิ่งเงื่อนไขที่เรนเดอร์**เฉพาะเมื่อเลือกโหมดแล้ว** แต่ค่าเริ่มต้นคือ
"ไม่ใช้" → เจ้าของงานจะไม่เห็นคำเตือนเลย · ชุดตรวจจับได้ (ทดสอบด้วย `mode='off'`) แล้วย้ายออกมานอกกิ่ง

นอกจากนี้ตัวเลขทุกจุด**คำนวณสดจากข้อมูล ไม่ hardcode** (ยอดเปลี่ยนทุกงวด) — ชุดตรวจยืนยันด้วยการ
คูณลูกหนี้ 2 เท่าแล้วดูว่าตัวเลขในคำเตือนขยับตาม (189.4 → 688.7 ลบ.) และยอดเดิมหายไปจริง

### เกร็ดที่ชี้แจงไปพร้อมกัน
เจ้าของงานแก้คำอธิบายของผู้ช่วยให้ถูก: "รีเฟรชแล้วกลับเป็น 4 แห่งเริ่มต้น" กำกวม — ที่ถูกคือชุด
**ผู้รับภาระกลับมาเป็น 12 (เดิม) + 4 (hub) = 16 แห่ง** · "4 แห่ง" หมายถึงฝั่ง hub เท่านั้น
บันทึกไว้ในคู่มือแล้วกันสับสนรอบหน้า + ชุดตรวจล็อกทั้งเลข 12 และ 16

---

## รอบปรับปรุง 5 — ตรวจ 2 ข้อสงสัยจากเจ้าของงาน (6 ส.ค. 69 · หลังเปิดดู localhost)

เจ้าของงานเปิดหน้าจริงแล้วแจ้ง 2 จุด — ตรวจแล้ว **1 จุดเป็นบั๊กจริง 1 จุดถูกอยู่แล้วแต่ UX กำกวม**

### ข้อ 1: "รพ.ปัว ไม่ขึ้นในตาราง Option พิเศษ" → ไม่ใช่บั๊ก แต่ต้องอธิบาย

**สาเหตุจริง**: จังหวัดน่าน **ไม่มี รพช. ระดับ 6-7 เลยสักแห่ง** พอขอบเขตเป็นค่าเริ่มต้น
"เฉพาะ รพช. วิกฤต 6-7" จึงไม่มีหนี้ก้อนไหนให้เกลี่ยในน่าน → ปัวได้ส่วนแบ่ง 0 → หายจากตาราง
(แม่ฮ่องสอนก็ไม่มี ศรีสังวาลย์จึงหายด้วยเหตุเดียวกัน · รพ.น่าน เองก็หาย)

เปลี่ยนขอบเขตเป็น **"รพช. ทุกแห่ง"** แล้วปัวขึ้นทันทีพร้อมส่วนแบ่ง **2.03 ลบ.**

**แก้ที่ UX**: เพิ่มบรรทัดอธิบายใต้ตารางว่า *"ผู้รับภาระ N แห่งไม่ขึ้นในตารางนี้"* พร้อมชื่อ รพ.
เหตุผลรายแห่ง และวิธีแก้ (เปลี่ยนขอบเขต) · ติดสัญลักษณ์ 🔁 กำกับตัวที่เป็น hub
— เดิมหายเงียบ ผู้ใช้จะนึกว่าเพิ่ม hub ไม่สำเร็จ

### ข้อ 2: "เงินสนับสนุนรวม 7.3M เหมือนไม่เปลี่ยนตามตัวกรอง" → ตัวเลขถูก แต่เจอบั๊ก cache ระหว่างตรวจ

**7.3 ลบ. คือค่าที่ถูกต้องของโหมด "🤝 ยกหนี้" + ขอบเขต "เฉพาะวิกฤต 6-7"**
ทดสอบครบทุกตัวกรองแล้ว ตัวเลข**ตอบสนองจริง**:

| ตัวกรอง | ผล |
|---|---|
| จังหวัด (เชียงใหม่ / น่าน) | 21.1 → **13.7 / 0** ✅ |
| เป้าระดับ (6 → 3 → 0) | 21.1 → **151.7 → 1,120** ✅ |
| โหมดตามจ่าย (ไม่ใช้/ยกหนี้/จ่ายจริง/เจาะจง) | 21.1 / **7.3 / 21.1 / 1.5** ✅ |
| เพิ่ม-ลด รพช. hub | **7.3 ↔ 1.5** ✅ |
| ขยายช่วงจำลอง +6 เดือน | 21.1 → **50.4** ✅ |

**⚠️ 2 ตัวที่ "ไม่ขยับ" — ตรวจแล้วถูกต้องทั้งคู่ ไม่ใช่บั๊ก**

1. **นิยาม MOE (Ver68/Ver69/+จ่ายหนี้การค้า) → 21,050,000.00 เท่ากันเป๊ะทุกเวอร์ชัน**
   ตอนแรกดูน่าสงสัยมาก ขุดถึงสูตรแล้วพบว่า**ถูกต้องทางคณิตศาสตร์** — MOE ถูกหักออกจาก NI สองที่
   แล้วหักล้างกันพอดี:
   ```
   NI = รายรับ − MOE − (คชจ.ทั้งหมด − MOE − non-cash) − ค่าเสื่อม
      = รายรับ − คชจ.ทั้งหมด + non-cash − ค่าเสื่อม        ← ไม่มี MOE เหลืออยู่เลย
   ```
   เพราะ "ค่าใช้จ่ายนอกนิยาม MOE" นิยามว่า = คชจ.ทั้งหมด − MOE − non-cash
   → **สลับเวอร์ชัน = ย้ายบัญชีข้ามถัง MOE ↔ นอกนิยาม เท่านั้น ยอดค่าใช้จ่ายรวมเท่าเดิม NI จึงเท่าเดิม**
   ยืนยันด้วยข้อมูลจริง: แม่วาง MOE 3.05 (Ver69) vs 3.31 (Ver68) แต่ NI = −0.76 ทั้งคู่
   ✅ ถูกต้องตามหลักบัญชี (การแบ่งหมวดไม่ทำให้กำไร/ขาดทุนเปลี่ยน) — โค้ดมีคอมเมนต์กำกับเจตนานี้ไว้แล้ว

2. **ตัวกรอง "เฉพาะวิกฤต 6-7" → 21.1 ลบ. เท่าเดิม** เพราะ รพ. ทั้ง 6 แห่งที่ต้องเติมเงิน
   อยู่ในกลุ่มวิกฤตอยู่แล้วทุกแห่ง (ระดับ 7 ทั้งหมด) กรองแล้วไม่มีใครหลุดออก ✅

3. **เดือนประเมินสภาพคล่อง (mmo)** ไม่ขยับ — ตั้งใจแยกจากกันตั้งแต่ 7.8⑦ ✅

### 🐞 บั๊กจริงที่เจอระหว่างตรวจ (ไม่มีใครรายงาน จับได้เอง)
`_refundCache` ของโหมด "ยกหนี้เจาะจง" cache ผล solver ด้วย signature ที่ **ไม่ได้นับ `EXTJHUB`**
→ เพิ่ม/ลบ hub แล้วได้ผลคืนเงินเก่าค้าง (hub เปลี่ยน → shares เปลี่ยน → sepRisk ของ hub
ซึ่งเป็นลูกหนี้ด้วย เปลี่ยน → เงินคืนที่ต้องใช้เปลี่ยน)

**สถานะ**: แก้แล้ว (ใส่ `hub:` เข้า signature) · เป็นบั๊กแฝง — ข้อมูลงวดนี้ hub ทั้ง 4 แห่ง
มีเงินคืนเป็น 0 หรือถูกจำกัดด้วย payIn ก้อนเล็ก ผลลัพธ์จึงยังไม่เพี้ยนให้เห็น แต่เป็นรูรั่วจริง
ที่จะโผล่เมื่อข้อมูลเปลี่ยนงวดหรือเพิ่ม hub ใหญ่

---

## รอบปรับปรุง 6 — "ปรับ %จ่ายหนี้การค้าแล้วเงินสนับสนุนไม่เปลี่ยน" (6 ส.ค. 69)

### เจ้าของงานถามซ้ำเป็นครั้งที่ 2
คำถามเดียวกับ Q1 ข้างบน (ถามหลัง deploy รอบ 1-2) — **การถามซ้ำคือสัญญาณว่าหน้าจออธิบายไม่พอ**
ไม่ใช่ว่าผู้ใช้ลืม · รอบนี้จึงไม่ตอบซ้ำอย่างเดียว แต่**วัดใหม่ทั้งหมดแล้วเอาคำอธิบายไปไว้บนหน้าจอ**

### วัดจริงแล้ว — คำตอบเดิมถูก และนี่คือตัวเลขยืนยัน (แม่วาง · จ่าย 100% → 0%)

| รายการ | จ่าย 100% | จ่าย 0% | ผล |
|---|---|---|---|
| MOE เงินสด/เดือน | 3.055 | 1.889 | ลดจริง ✅ |
| MOE คงค้าง/เดือน | 3.055 | 3.055 | เท่าเดิม (ตามเจตนา) |
| เงินสดปลาย ก.ย. | 8.391 | 11.887 | **+3.496** |
| เจ้าหนี้ (CL) ปลาย ก.ย. | 46.902 | 50.398 | **+3.496 เท่ากันเป๊ะ** |
| **NWC ณ ก.ย.** | −20,956,132 | −20,956,132 | **เท่ากันทุกบาท** |
| **NI สะสม ณ ก.ย.** | −9,128,624 | −9,128,624 | **เท่าเดิม** |
| CR / QR / Cash ratio | .553/.497/.179 | .584/.532/.236 | ดีขึ้นเล็กน้อย |
| ระดับ ณ ก.ย. | 7 | 7 | ไม่ขยับ |
| **เงินสนับสนุน** | 9.15 | 9.15 | **เท่าเดิม** |

**เหตุผล**: ยืดหนี้ = ไม่จ่ายเงินสดออก แต่ไปกองเป็นเจ้าหนี้การค้า**เท่ากันเป๊ะ** → CA เพิ่ม X, CL เพิ่ม X
→ **NWC = CA − CL เท่าเดิม** · ส่วน NI เท่าเดิมเพราะเกณฑ์คงค้าง (ค่าใช้จ่ายเกิดแล้ว เพียงยังไม่จ่าย)
รพ. ทั้ง 6 แห่งที่ต้องเติมเงิน **ติดเกณฑ์ NWC ติดลบ + NI ติดลบ ซึ่งการยืดหนี้แตะไม่ได้ทั้งคู่**
→ เงินก้อนที่ต้องเติมให้พ้นระดับ 6 จึงเท่าเดิม · ✅ **ถูกต้องทางเศรษฐศาสตร์ — ยืดหนี้ไม่ใช่เงินใหม่**

### กลไกไม่ได้ตายด้าน — พิสูจน์แล้วว่าเชื่อมกันจริง
สแกนครบ 103 แห่งที่ช่วงจำลอง +9 เดือน ปรับ % จาก 100→0:
- **เงินสดปลายงวดขยับ 103 แห่ง** ✅
- **ระดับวิกฤตขยับ 24 แห่ง** ✅
- เงินสนับสนุนขยับ **1 แห่ง** (ดอยหล่อ) · ที่โหมด "ไม่ใช้" +12 เดือน ยอดรวมขยับ −0.9 ลบ.

แปลว่าท่อต่อกันครบ เพียงแต่ รพ. ที่ต้องเติมเงินติดเกณฑ์ที่ยืดหนี้ช่วยไม่ได้

### สิ่งที่แก้ (แก้ที่หน้าจอ ไม่ใช่แก้สูตร)
เพิ่มข้อความข้างช่องกรอก % ตอนเลือกโหมด "จ่ายหนี้การค้าบางส่วน":
> ค้างชำระไว้ X/ด. → เจ้าหนี้การค้าโตเดือนละเท่านี้ · NI ไม่เปลี่ยน ·
> **💰 เงินสนับสนุนมักไม่ขยับตาม %** (ยืดหนี้ไม่ใช่เงินใหม่ — CA กับ CL โตเท่ากัน NWC/NI จึงเท่าเดิม) ℹ️

พร้อม tooltip กางเต็มอธิบาย 5 บรรทัด · แสดงเฉพาะโหมด 69p เท่านั้น (โหมดอื่นไม่รก)

### ⚠️ ยังค้าง
เจ้าของงานเห็นยอด **62.9 ลบ.** บนจอ — สแกน 80 คอมบิเนชัน (โหมด×ขอบเขต×ช่วงจำลอง×ตัวกรอง)
แล้ว**ยังหาค่านี้ไม่เจอ** แปลว่ามีการปรับอย่างอื่นร่วมด้วย (%รายรับ / %MOE รายกลุ่ม / กำหนดเงินเองรายแห่ง)
— ไม่กระทบคำอธิบายข้างบน (กลไกเดียวกันทุก config) แต่ยังไม่ได้ยืนยันตรงกับจอจริง

---

## รอบปรับปรุง 7 ⭐ — 🔄 โยกเงินช่วยกันระหว่าง รพ. (6 ส.ค. 69 · ฟีเจอร์ใหม่)

### ปัญหาที่เจ้าของงานจับได้
ลองเปลี่ยน 💧 ประเมินสภาพคล่อง จาก 3 → 13 เดือน แล้วพบว่า "รวมเงินเติมตามสภาพคล่อง"
พุ่งจาก **115.8 ลบ. → 7.08 พันล้าน** — **ระบบสร้างภาพว่าเติมเงินหลายพันล้านโดยไม่บอกว่าเงินมาจาก รพ. ใด**

> "ความถ้าไม่ทำแบบนี้ ระบบจะสร้างภาพว่าเติมเงินหลายล้านแต่ไม่รู้ว่ามีที่มาจาก รพ.ใด"

### ข้อมูลที่ยืนยันว่าเจ้าของงานคิดถูก
| ช่วงประเมิน | กำลังช่วยจริงทั้งเขต | ต้องการ | ผล |
|---|---|---|---|
| 3 เดือน | 2,610 ลบ. | 236 ลบ. | ✅ 7/8 จังหวัดช่วยกันเองพอ (ลำพูนขาด 22.4) |
| 13 เดือน | 352 ลบ. | 12,017 ลบ. | ❌ **ขาด 11,665 ลบ. ไม่มีต้นทางในเขตเลย** |

### กติกาที่เจ้าของงานเคาะ (ถาม 4 ข้อ ตอบครบ)
| ประเด็น | คำตอบ |
|---|---|
| ขอบเขตการโยก | **ข้ามจังหวัดได้เสรี** (แต่ระบบเรียงในจังหวัดขึ้นก่อน + ติดป้าย "ข้ามจังหวัด") |
| เพดานผู้ให้ | **หยุดที่ระดับ 5 · เตือนตั้งแต่เกิน 3** |
| บันทึกแผน | **ถาวร** (เซฟ localStorage — ต่างจาก hub ที่ชั่วคราว) |
| ปุ่มจัดสรรอัตโนมัติ | **เอา** — เสนอให้ก่อนแล้วแก้มือได้ |

### สิ่งที่ทำ
- `EXST.xfer=[{f,t,a}]` — แผนโยก เก็บถาวร · helper `exXferIn/Out/Net`
- **เชื่อมเข้าเครื่องยนต์ที่จุดเดียว**: ใส่ `exXferNet(h)` เข้า `inj` ต้น `exSimPath()` → ทุกคอลัมน์
  (สภาพคล่อง/ระดับวิกฤต/Solver/เพดาน) รับผลอัตโนมัติ · **ไม่ต้องเขียนสูตรใหม่เลย** เพราะโมเดลเดิม
  รับ inj ทั้งบวก-ลบอยู่แล้ว (ผู้รับ = รายได้เงินอุดหนุน · ผู้ให้ = จ่ายเงินอุดหนุนออก) สมมาตรพอดี
- **เพดาน** `exXferCap(h)` = min( เงินสดเหลือหลังภาระ MOE , `exSolveDown(h,6)−1step` )
  ⚠️ สองค่านี้มาจากคนละสมมติฐาน (①ไม่มีรายรับเลย ②นับรายรับจริง) จงใจเอาค่าที่ต่ำกว่า = ระวังทั้งสองทาง
- **UI**: ปุ่ม 🔄 โยกช่วย ในคอลัมน์เงินสดคงเหลือฯ → กางแผง (dropdown ปลายทาง + วงเงิน +
  ปุ่มโอน + ปุ่มเติมเต็มส่วนขาด) · ฝั่งผู้รับขึ้นป้าย **`+ชื่อรพ. xxx ✕`** ในคอลัมน์ส่วนขาด กด ✕ ยกเลิกได้
- **⚡ จัดสรรอัตโนมัติ** — greedy: เติมให้แห่งที่ขาดมากก่อน ดึงจากผู้ให้ในจังหวัดเดียวกันก่อน แล้วค่อยข้ามจังหวัด
- **บรรทัดสรุป** — ตัวเลข "ไม่ Over" ที่ต้องการ: `โยกแล้ว X · กำลังช่วยที่เหลือ Y · ขาดต้นทาง Z (ต้องเป็นเงินนอกเขต)`

### ผลลัพธ์ที่ได้ (3 เดือน · กดจัดสรรอัตโนมัติ)
- โยก **38 รายการ รวม 236.2 ลบ.** → ส่วนขาดสภาพคล่อง **236.2 → 0**
- **เงินสนับสนุนที่ต้องขอนอกเขตลดจาก 21.05 → 4.25 ลบ. (−80%)** เพราะการโยกช่วยยกระดับผู้รับด้วย
- ข้ามจังหวัดแค่ 5/38 รายการ · ผู้ให้ทุกแห่งระดับ ≤5 · ไม่มีผู้รับได้เกินส่วนที่ขาด
- ที่ 13 เดือน ระบบ**ไม่แกล้งเติมให้ครบ** — โยกได้ 352.3 ลบ. แล้วขึ้นว่า **"ขาดต้นทาง 11.66 พันล้าน"**

### ✂️ ย่อข้อความในเซลล์ (ขอมาพร้อมกัน)
`กรณีรายรับไม่เป็นไปตามแผน · ภาระ MOE 3 ด. …` → **`สมมติไม่มีรายรับ · MOE 3ด. …`**
และ `กรณีรายรับปกติ: …` → **`ปกติ: …`** · ความยาวเหลือ **77 ตัวอักษร** · ยังแยก 2 สมมติฐานได้ชัดตาม 7.8⑧
(คำอธิบายเต็มย้ายไปอยู่ tooltip หัวคอลัมน์)

### ชุดตรวจ
`test_exec_xfer.js` (ใหม่) 9 หัวข้อ — helper/ผลต่อคอลัมน์ · เข้าแบบจำลองจริงทั้งสองฝั่ง · เพดานระดับ 5 ·
กันกรอกเกิน/โยกซ้ำ/โยกให้ตัวเอง · จัดสรรอัตโนมัติทั้งกรณีเงินพอ (3 ด.) และไม่พอ (13 ด.) ·
บันทึกถาวรจริง · UI ครบ · ข้อความสั้นลงจริง — **ผ่านทุกข้อ · รวม 6 ไฟล์ผ่านหมด**

---

## สถานะ ณ ตอนนี้

- **รอบ 1-2 deploy แล้ว** — commit `336a1c8` · ยืนยัน byte-identical กับ URL จริง + รันชุดตรวจกับไฟล์ที่ deploy จริงแล้ว
- **รอบ 3-4 push แล้ว** — commit `64375c7` (6 ส.ค. 69) · ⚠️ GitHub Pages ยังไม่ rebuild ตอนที่บันทึกนี้
- **รอบ 5 push แล้ว** (6 ส.ค. 69) — แก้ตามที่เจ้าของงานแจ้ง 2 ข้อ
- **รอบ 6-7 push แล้ว** (6 ส.ค. 69) — คำอธิบาย %จ่ายหนี้การค้า + ฟีเจอร์โยกเงินช่วยกัน
- ชุดตรวจที่เกี่ยวข้อง: `test_exec_model.js` · `test_wl_moe.js` · `test_exec_columns.js` · `test_moe_ver.js`
  · `test_exec_tj_hub.js` (รอบ 3-6) · `test_exec_xfer.js` (ใหม่รอบ 7) — **ผ่านทุกข้อทั้ง 6 ไฟล์**
- เอกสารสูตร/กฎเต็ม: `RISK_EXEC_MODEL.md` หัวข้อ **3.14** (นิยาม MOE) และ **7.9** (รอบ 3-7)
- **งานค้างที่ควรถาม CFO/สสจ. ต่อ**:
  1. ส่วนต่างลูกหนี้−เจ้าหนี้ 189.4 ลบ. คู่หนี้จริงเป็นใคร (ต้องขอตารางเคลียร์ริ่งของ สสจ./เขตมายืนยัน)
  2. รายชื่อ รพช. hub ควรเป็น 4 แห่งนี้ถาวรหรือมีเพิ่ม (ตอนนี้แก้ชั่วคราวได้ แต่ไม่บันทึกถาวร)
  3. น่าน/แม่ฮ่องสอน ไม่มี รพช. ระดับ 6-7 เลย → Option ตามจ่ายในขอบเขต "เฉพาะวิกฤต" ใช้กับ 2 จังหวัดนี้
     ไม่ได้ ต้องใช้ขอบเขต "ทุกแห่ง" — ควรตัดสินใจว่าจะให้ค่าเริ่มต้นเป็นอันไหน

---

## รอบปรับปรุง 8 — 🔍 ตรวจทั้งสายตามที่เจ้าของงานสั่ง + ปิดคอลัมน์ "เงินที่ยกให้" (6 ส.ค. 69)

### โจทย์
เจ้าของงานสั่งให้ตรวจทั้ง logic และกระบวนการ ตั้งแต่ระดับวิกฤตปัจจุบัน ไล่ทีละคอลัมน์จนถึง
ระดับหลังช่วย พร้อมคำถามเฉพาะ: **"เงินที่ยกให้" ถูกนำไปคำนวณซ้ำหรือเปล่า**

### ✅ ผลตรวจ: ไม่มีการคิดซ้ำ (ยืนยัน 2 ทาง)
1. **ทางข้อมูล** — ยกหนี้ D บาท แล้ววัด: ΔCL = **−D พอดี** · ΔNI = **+D พอดี** (ไม่ใช่ −2D/+2D)
   ทดสอบ 6 แห่ง ตรงทุกแห่ง
2. **ทางโค้ด** — `exTjGive()` **ไม่เคยถูกเรียก** ใน `exSimPath` / `exSolveFor` / `exSolveDown` / `exMoeLeft`
   ทั้งไฟล์อ้างถึงแค่ 2 จุด: นิยามตัวเอง + ส่งไปแสดงผล
   ผลของ Option ตามจ่ายถูกใส่เข้าแบบจำลอง**โดยตรงจาก `EXTJ.debtors`/`EXTJ.shares` ครั้งเดียว**

→ ทำตามที่เจ้าของงานสั่ง: **ปิดการแสดงคอลัมน์** (`EX_SHOW_GIVE=false`) ไม่ต้องแก้สูตรใด ๆ
เปิดกลับได้ทันที · ชุดตรวจนับคอลัมน์เป็นไดนามิกตามธงแล้ว (ไม่ hardcode)
→ เพิ่มคอลัมน์ TSV แทน: **รับโยกจาก รพ.อื่น / รับโยกจาก(ชื่อ) / โยกออกให้ รพ.อื่น** ตามที่สั่งว่า
"อะไรที่ใช้คอลัมน์เงินที่ยกให้ ให้ไปใช้ตัวเลขจากส่วนขาดสภาพคล่องแทน"

### 🔍 ผลตรวจทั้งสาย (ตัวอย่าง แม่วาง · รพช. เชียงใหม่ · งวด 256909 เดือนที่ 9)
| # | คอลัมน์ | ค่า | ที่มา/สูตร | ผล |
|---|---|---|---|---|
| ① | ระดับปัจจุบัน | **7** | `summary.json` src=`gl` (คำนวณจากงบทดลอง ไม่ใช่ทางการ) · ตรวจย้อนด้วย `scoreOf()` ได้ 7 เท่ากัน | ✅ |
| ② | MOE/เดือน | **3.055** | Σ 7 กลุ่มบัญชี Ver69 ÷ เดือน (labor .803 · med 1.119 · svc .772 …) | ✅ |
| ③ | NI จำลอง | **−0.761** | รายรับ 10.493 − MOEคงค้าง 3.055 − คชจ.นอกนิยาม 7.454 − ค่าเสื่อม 0.746 | ✅ สมการปิด |
| ④ | เงินสด+เทียบเท่า | **8.584** | RatioItemID **1003X** ตามผังบัญชี · **19 บัญชี 5 กลุ่ม** (เงินสดในมือ/ฝากคลัง/ฝากธนาคาร/เทียบเท่าอื่น/ฝากประจำ) | ✅ ตรงผัง |
| ⑤ | เงินสดคงเหลือหลังภาระ MOE | **−8.457** | 8.584 − เจ้าหนี้ 8.802 + ลูกหนี้ 0.924 + โยกสุทธิ 0 − MOE×3 9.164 | ✅ ตรงสูตร |
| ⑥ | ส่วนขาดสภาพคล่อง | **8.457** | ส่วนติดลบของ ⑤ · เพดานโยกให้คนอื่น = **0** (ติดลบอยู่ จึงให้ใครไม่ได้) | ✅ |
| ⑦ | ระดับ ก.ย. ก่อนช่วย | **7** | เดินเดือนถึง ก.ย. แล้วให้คะแนนด้วย `scoreOf` ชุดเดียวกับ ① | ✅ |
| ⑧ | เงินสนับสนุน | **9.150** | Solver binary search (SV_STEP 50,000) | ✅ |
| ⑨ | ระดับ ก.ย. หลังช่วย | **6** | ถึงเป้าพอดี | ✅ |

**เกณฑ์ Risk Score ที่ใช้** (ตรงสูตรทางการ): LI 3 คะแนน (CR<1.5, QR<1.0, Cash<0.8) +
ST 2 คะแนน (NWC<0, NI<0) + SU 0-2 (4-quadrant NWC เทียบ NI) = เต็ม 7 ·
ปัด 2 ตำแหน่งก่อนเทียบเกณฑ์ให้ตรงกับที่ HFO ทำ

### แผงกดเข้าไปดู — มีอยู่แล้ว ไม่ต้องเพิ่ม
คลิก badge คอลัมน์ "ระดับ ก.ย.69 หลังช่วย" → แผงเจาะเกณฑ์ 7 คะแนน แสดง
**"💰 เงินก้อนขั้นต่ำเพื่อให้ถึงเป้าระดับ L ภายใน ก.ย.69: 9.2M"** พร้อมปุ่มเลือกดูระดับเป้าอื่น 0-7
และตาราง ปัจจุบัน → ก่อนช่วย → หลังช่วย รายเกณฑ์


---

## รอบปรับปรุง 9 — ลูกหนี้ปรับลดได้ + ส่วนขาดไม่หายไป + โหมดกว้าง (6 ส.ค. 69)

### ① ลูกหนี้ UC-OP นอก CUP ปรับลดได้
**เหตุผลจากเจ้าของงาน**: ฝั่ง**เจ้าหนี้เคลียร์เป็น 0 ได้แน่นอนทั้งเขต** เพราะเป็นหนี้กันเองในเครือข่าย
**สป.สธ.** แต่ฝั่ง**ลูกหนี้เกินมา 189 ลบ.** ซึ่งบางส่วนน่าจะเป็นกรมอื่น = **อาจเรียกเก็บไม่ได้**
และยังไม่รู้ว่าที่ไหนเก็บไม่ได้ → ขอให้ปรับลดด้วยมือรายแห่งได้

**ที่ทำ** — 2 ชั้น (idiom เดียวกับ %ปรับรายรับ/MOE ที่มีอยู่):
- **`arPct`** % ที่คาดว่าเก็บได้ รวมทั้งเขต (ช่องกรอกเหนือตาราง · ค่าเริ่มต้น 100)
- **`arOvr[hcode]`** กำหนดเองรายแห่งเป็นบาท — กด **✎** ในเซลล์ · **ชนะ % รวม**
- กรอกเกินยอดในบัญชีไม่ได้ (จำกัดที่ยอดจริง) · เซลล์ที่ถูกลดขึ้น **"ตัดออก X"** สีทอง
- ปุ่ม **คืนค่าเต็มทั้งหมด** ล้างทั้ง % และรายแห่ง · **เซฟถาวร**

ทดสอบ: 60% → ลูกหนี้ 499.3 → 299.6 ลบ. และ**ส่วนขาดสภาพคล่องเพิ่มจาก 236.2 → 282.6 ลบ.** ตามจริง

### ② ส่วนขาดสภาพคล่อง — ไม่ให้ตัวเลขหายไปเมื่อได้รับโยก
**ปัญหา**: พอโยกเงินเข้าไป ตัวเลขลดจนหายไป ทำให้ไม่รู้ว่า รพ. นี้เคย**ต้องการเติมเงิน**เท่าไหร่
และ**รวมแล้วได้รับสนับสนุนกี่บาท**

**ที่ทำ** — แยกเป็น 2 ตัวเลข:
- **`exTopUpGross`** = ความต้องการ**ตั้งต้น** (ถอดเฉพาะเงินที่รับโยกมา) → **เป็นตัวเลขหลักในคอลัมน์**
- **`exTopUp`** = ที่**ยังขาดอยู่จริง** → แสดงเป็นบรรทัดย่อย "ยังขาด X" หรือ "✅ ครบแล้ว"
- บรรทัดย่อย **"รับแล้ว X จาก N แห่ง"** + รายชื่อผู้ให้
- **ชิป 💧 รวมเงินเติมตามสภาพคล่อง ด้านบนใช้ Gross** → จัดสรรครบทุกแห่งแล้ว**ยอดไม่กลายเป็น 0**
  (ยืนยัน: จัดสรรอัตโนมัติครบแล้วชิปยังแสดง 236.2M / 35 แห่ง)
- ตัวเรียงลำดับคอลัมน์นี้ใช้ Gross ด้วย

### ③ เลย์เอาต์ — ป้ายเรียงลงล่าง + โหมดกว้าง
- **ป้ายผู้สนับสนุนเรียงลงล่างทีละบรรทัด** (`.xfin` เป็น flex block · `max-width:190px`)
  ไม่ต่อไปทางขวาจนตารางยืดบังคอลัมน์อื่นอีกแล้ว
- **↔️ โหมดกว้าง** (checkbox เหนือตาราง · เซฟถาวร) — ซ่อนคอลัมน์จังหวัด + ย่อคอลัมน์ชื่อ รพ.
  (`max-width:120px` ตัดคำ ย้ายจังหวัดไปเป็นบรรทัดย่อย) · `min-width` ตาราง 1000 → 860px
- **🪤 กับดักที่ต้องแก้ตาม**: คอลัมน์ที่ตรึงซ้าย (sticky) ผูกกับ `nth-child(2)` — พอซ่อนจังหวัด
  ชื่อ รพ. เลื่อนมาเป็นคอลัมน์ที่ 1 จะ**ตรึงผิดคอลัมน์** → เพิ่มคลาส `.ex-wide` ย้าย sticky
  มาที่ `nth-child(1)` และคลายที่ `nth-child(2)` · ชุดตรวจล็อกทั้งสองกฎ

### ชุดตรวจ
`test_exec_xfer.js` เพิ่มหัวข้อ 10-12 (รวมเป็น 12 หัวข้อ) — **ผ่านทุกข้อ · 6 ไฟล์ผ่านหมด**

---

## รอบปรับปรุง 10 — ป็อปอัปโยกเงิน + ปุ่มยุบรายชื่อให้หาเจอ + ชิปนับเฉพาะที่โยกจริง (6 ส.ค. 69)

### ① แผงโยกเงิน → ป็อปอัป
**ปัญหา**: แบบกางเป็นแถวย่อยในตาราง **ใหญ่เกินไป กดแล้วหน้าต่างเพี้ยน**
**แก้**: เปลี่ยนเป็นป็อปอัปกลางจอ — ใช้ pattern `.nip-overlay`/`.nip-modal` **ที่หน้านี้มีอยู่แล้ว**
(ตัวเดียวกับ niPlanOverlay/debtPlanOverlay) ไม่ได้สร้างระบบใหม่ · กว้างสูงสุด 640px เลื่อนในตัวเองได้
· ปิดด้วยปุ่ม ✕ หรือคลิกพื้นหลัง · **ถอดแถวย่อยในตารางออกหมด** ตารางไม่ยืด/ไม่เพี้ยนอีก
· เปิดค้างไว้แล้วกดโอน → `exRender()` วาดป็อปอัปใหม่ให้ตัวเลขเพดาน/รายการสดเสมอ

### ② ปุ่มยุบรายชื่อ รพ. — เจ้าของงาน "ไม่เห็นจะยุบหรือซ่อนได้เลย"
รอบก่อนทำเป็น **checkbox "↔️ โหมดกว้าง" ในแถวควบคุมที่มีของเยอะ → หาไม่เจอ** (บทเรียน: ฟีเจอร์ที่
ผู้ใช้ต้องมองหา ต้องวางติดกับสิ่งที่มันแก้ ไม่ใช่รวมไว้ในแถบตัวเลือกรวม)

**แก้**: ย้ายเป็น **ปุ่มเด่นวางเหนือตารางโดยตรง**
`◀ ยุบรายชื่อ รพ. (เพิ่มพื้นที่)` ⇄ `▶ กางรายชื่อ รพ. กลับ`
พร้อมข้อความข้าง ๆ อธิบายว่ากดแล้วได้อะไร · ถอด checkbox เดิมออก (เหลือทางเข้าเดียว ไม่สับสน)

### ③ ชิป 💧 = ยอดที่จัดสรรด้วยการโยกเงินจริงเท่านั้น
**คำสั่งเจ้าของงาน**: "ตัวเลขรวม 💧 จะต้องเป็นตัวเลขรวมจากตัวเลขที่จัดสรรด้วยการโยกเงินเท่านั้น"

| | ก่อน | หลัง |
|---|---|---|
| ชิป 💧 | 280.1M (38 แห่ง) = ความต้องการตั้งต้น | **Σ เงินที่โยกจริง** · ยังไม่โยก = **0** |
| สี | แดง (เหมือนคำของบ) | เทา→**เขียว**เมื่อมีการโยกแล้ว |
| ความต้องการตั้งต้น | เป็นตัวเลขหลัก | ย้ายไปในวงเล็บ **"จากที่ต้องการ X"** |

→ **ทุกบาทในชิปมีต้นทางระบุชื่อ รพ. ผู้ให้เสมอ ไม่มียอดลอยอีกแล้ว** ซึ่งเป็นเป้าหมายตั้งต้นของ
เจ้าของงานตั้งแต่รอบ 7 · ยืนยัน: ยังไม่โยก = 0 · จัดสรรอัตโนมัติแล้ว = 236.2M เท่ายอดที่โยกจริงพอดี

### ชุดตรวจ
ปรับ `test_exec_columns.js` (ชิปเปลี่ยนความหมาย + regex ยืดหยุ่นรับวงเล็บใหม่ · จังหวัดที่ไม่มี
ส่วนขาดเลยไม่มีวงเล็บ = ถูกต้อง) และ `test_exec_xfer.js` (แผงเป็นป็อปอัป · ชิปนับเฉพาะที่โยกจริง
· ปุ่มยุบรายชื่อ) — **ผ่านทุกข้อทั้ง 6 ไฟล์**

---

## รอบปรับปรุง 11 — ยุบแถบรายชื่อ รพ. ด้านซ้ายของจอ (6 ส.ค. 69)

### ผู้ช่วยเข้าใจผิดมา 2 รอบ — เจ้าของงานส่งภาพวงกลมมาให้จึงเข้าใจตรงกัน
เจ้าของงานบอกว่า "List รายชื่อโรงพยาบาลด้านซ้ายของจอ" มา 2 รอบ แต่ผู้ช่วย**ตีความเป็นคอลัมน์
จังหวัด+ชื่อ รพ. ที่ตรึงอยู่ในตาราง (sticky)** ทั้งสองรอบ จึงทำ "โหมดกว้าง" ที่ไม่ตรงโจทย์
พอเจ้าของงาน**ส่งภาพหน้าจอวงกลมให้** จึงเห็นว่าหมายถึง **`.left` = แถบด้านซ้ายของทั้งหน้า**
(ช่องค้นหา + ตัวกรองระดับ/จังหวัด + แท็บ Risk สูงสุด/แย่ลงเร็วสุด + รายการ รพ. เรียง 1,2,3…)
กว้าง **340px** ซึ่งกินพื้นที่มากที่สุด

> **บทเรียน**: เมื่ออธิบายตำแหน่งบนหน้าจอแล้วผู้ช่วยทำผิดซ้ำ — ขอภาพประกอบเร็วกว่าอธิบายซ้ำ

### สิ่งที่ทำ
- ปุ่ม **◀/▶ ลอยติดขอบซ้ายของพื้นที่เนื้อหา** (แถบแนวตั้งบาง 18×74px) เห็นตลอดเวลา ไม่ต้องหา
- กดแล้ว `.main` ได้คลาส `lcol` → `.left` กว้าง **340px → 0** แบบ **สไลด์** (`transition:width .18s`)
  ตามที่เจ้าของงานเสนอ ("อาจจะสไลด์ไปด้านข้างก็ได้นะ") → คืนพื้นที่ 340px ให้ตารางเต็ม ๆ
- **จำสถานะไว้** (`localStorage` คีย์ `rd_leftcol` — **แยกคีย์** ไม่ปนกับ state ของแท็บ #exec)
- **มือถือ/แท็บเล็ต (≤900px) ซ่อนปุ่มนี้** เพราะมี drawer เดิมอยู่แล้ว — ไม่ให้ 2 ระบบชนกัน

### แก้ป้ายปุ่มเก่าไม่ให้สับสน
ปุ่มที่ทำรอบก่อน (ย่อคอลัมน์ในตาราง) ยัง**มีประโยชน์และเก็บไว้** แต่เปลี่ยนป้ายจาก
"◀ ยุบรายชื่อ รพ." → **"◀ ย่อคอลัมน์ชื่อในตาราง"** และเพิ่มข้อความบอกว่า
*"ถ้าจะยุบแถบรายชื่อ รพ. ทั้งแผง กดปุ่ม ◀ ที่ขอบจอด้านซ้าย"* — ตอนนี้มี 2 ปุ่มคนละหน้าที่ชัดเจน

### ชุดตรวจ
`test_exec_xfer.js` หัวข้อ 12 เพิ่ม 7 ข้อ (ยุบแล้วกว้าง 0 · ปุ่มลอยติดขอบ · มี transition ·
มือถือซ่อนปุ่ม · มีฟังก์ชัน toggle · จำสถานะ · drawer เดิมไม่ถูกทับ) — **ผ่านทุกข้อ · 6 ไฟล์ผ่านหมด**

---

## รอบปรับปรุง 12 ⭐⭐ — เจ้าของงานจับได้ว่าโมเดล "มองโลกสวย" (6 ส.ค. 69)

### เจ้าของงานทัก
> "เช็คระดับ ก.ย.69 ก่อนช่วย กับ หลังช่วย อีกที ฉันว่ามันไม่น่าจะเป็นระดับ 0 ได้นะ"

**ถูกต้อง และปัญหาใหญ่กว่าที่เห็น** — ทำ backtest กับข้อมูลจริง 2 ปีแล้วพบว่าโมเดล**ทำนายผิดทิศ**

| | ด.9 → ด.12 | ระดับ 0 | ดีขึ้น/แย่ลง |
|---|---|---|---|
| ปี 2567 **จริง** | 25 → 17 แห่ง | −8 | 21 / **33** |
| ปี 2568 **จริง** | 34 → 11 แห่ง | −23 | 7 / **65** |
| ปี 2569 **โมเดลเดิม** | 41 → 53 แห่ง | **+12** | 29 / **0** |

โมเดลเดิมบอก **ไม่มี รพ. แย่ลงเลยสักแห่ง** ทั้งที่ 2 ปีติดกันสถานการณ์แย่ลงเป็นหลัก

### สาเหตุ 2 ข้อ (วัดจากข้อมูลจริง)
**① NI ไตรมาสสุดท้ายพลิกลบหนักทุกปี** (ปิดบัญชีปลายปี: ตั้ง คชจ.ค้างจ่าย · ตัดลูกหนี้ · ค่าเสื่อมงวดท้าย)

| ปีงบ | เฉลี่ย 9 ด.แรก | เฉลี่ย 3 ด.ท้าย | ต่าง |
|---|---|---|---|
| 2567 | +83 ลบ./ด. | **−245** | −328 |
| 2568 | +191 ลบ./ด. | **−651** | −843 |
| 2569 | +259 ลบ./ด. | โมเดลเดิมลาก **+259** ต่อ | **0** ⚠️ |

**② โมเดลตรึงหนี้สินหมุนเวียนคงที่ 103/103 แห่ง** → CR/QR/Cash ดีขึ้นเองเพราะเงินสดพอกฝ่ายเดียว
ของจริง CL โต +2.3% (ปี67) และ **+7.8%** (ปี68) · โตขึ้น 56 และ 72 แห่งตามลำดับ

### สิ่งที่แก้ (เจ้าของงานเคาะ: "ใส่ปัจจัยฤดูกาลจากข้อมูลจริง" + "รับได้ เอาความถูกต้องก่อน")
- **`bs.niYE`** — ปัจจัยฤดูกาล NI ปลายปีรายแห่ง (เฉลี่ยจริงปี 67+68) รวมทั้งเขต **−585 ลบ./ด.**
  (อยู่กึ่งกลางระหว่าง −328 กับ −843 พอดี) · ใช้เฉพาะเดือน ≥10
  ⛔ **ไม่ให้ไปลดเงินสด** เพราะรายการปิดบัญชีส่วนใหญ่เป็นเกณฑ์คงค้าง — ผลต่องบดุลวัดแยกที่ clYE
- **`bs.clMo` / `bs.clYE`** — อัตราโตหนี้สินหมุนเวียนรายแห่ง (run-rate ต้นปี / อัตราปลายปีจริง)
  ปลายปีโตเร็วกว่าต้นปี 34 ลบ./ด.
- **ตัวเลือกเปิด/ปิด 2 ตัว** เหนือตาราง (📉 ฤดูกาลปลายปีงบ · 📊 หนี้สินเดินตามจริง) ให้ CFO เทียบได้

### ผลลัพธ์ — ทิศทางถูกแล้ว
| | ดีขึ้น | แย่ลง | ระดับ 0 | เงินสนับสนุน |
|---|---|---|---|---|
| โมเดลเดิม | 29 | **0** | 53 | 21.05 ลบ. |
| **โมเดลใหม่** | 17 | **29** | 36 | **57.25 ลบ.** |
| ของจริงปี 67 | 21 | 33 | 17 | — |
| ของจริงปี 68 | 7 | 65 | 11 | — |

**เงินสนับสนุนที่ต้องใช้เพิ่มจาก 21.05 → 57.25 ลบ. (11 แห่ง)** — ตัวเลขเดิมประเมินต่ำไปเกือบ 3 เท่า

### 🪤 ผู้ช่วยพลาดระหว่างตรวจ 2 ครั้ง (บันทึกไว้เตือน)
เขียนสคริปต์ทดสอบผิดจนเกือบรายงานว่า "ปรับ %รายรับแล้วไม่มีผล" ทั้งที่โมเดลถูก —
① เซ็ต `adjAll` ซึ่งเป็นแค่ค่าจำของสไลด์ (ตัวจริงคือ `EXST.adj[key]`)
② ใช้ `EXROWS_SEGS` ที่ยังว่าง เพราะถูกสร้างใน `exRender()` เท่านั้น
**บทเรียน: ก่อนสรุปว่า "ฟีเจอร์ไม่ทำงาน" ต้องพิสูจน์ว่าสคริปต์ทดสอบเซ็ตค่าเข้าจริง**

### ชุดตรวจ
**`test_exec_backtest.js` (ใหม่)** — เทียบโมเดลกับของจริงปี 67/68 + ล็อกว่าต้องทำนาย "แย่ลง ≥10 แห่ง"
และระดับ 0 ต้องไม่เกิน 45 แห่ง + ตัวเลือกฤดูกาลต้องมีผลจริง (0 → 29 แห่ง)
· `test_exec_model.js` เปลี่ยนค่าฐานเป็น 57.25M/11 แห่ง และ**เปลี่ยนจากเตือนเฉย ๆ เป็น fail จริง**
— **ผ่านทุกข้อทั้ง 7 ไฟล์**

---

## รอบปรับปรุง 4 — 🚨 ปัจจัยฤดูกาลหายทั้งชุดตอนเดินงวด แล้วไม่มีอะไรพัง (11 ส.ค. 69)

### อาการที่เจ้าของงานจับได้เอง
เปิดหน้าเว็บจริงแล้วทักว่า **"เงินสนับสนุนที่ต้องใช้ 50.85 ลบ. น้อยไปมาก"** — ถูกต้อง

### สาเหตุ
รอบปรับปรุง 3 (`b345a64`) เพิ่ม `bs.niYE` / `bs.clYE` เข้าโมเดล **แต่แก้ `export_exec.py` แค่ `clMo`
ตัวเดียว** ค่าฤดูกาลสองตัวถูกใส่ลง `exec.json` งวด 256909 ด้วยมือ ไม่ได้อยู่ในไพป์ไลน์
→ พอ export งวด 256910 (ก.ค. 69) เมื่อ 10 ส.ค. คีย์หายทั้ง 103 แห่ง

**สิ่งที่น่ากลัวคือมันไม่พังให้เห็น** — หน้าเว็บเรนเดอร์ปกติ สวิตช์ 📉 ยังกดได้แต่เป็น no-op
(`niYEV = 0` · `clYE` ตกกลับไปใช้ `clMo`) ตัวเลขที่ออกมาก็ยัง "ดูสมเหตุสมผล" จนไม่มีใครสงสัย

### ทำไมชุดตรวจไม่จับ
`test_exec_backtest.js` มีข้อ "ตัวเลือกฤดูกาลต้องมีผลจริง" อยู่แล้ว แต่ปิด `seas` **พร้อมกับ**
`clGrow` แล้ววัดรวม — `clGrow` ตัวเดียวก็ทำให้ผลต่างแล้ว ข้อนี้จึงยัง ✅ ทั้งที่ `seas` ตายไปแล้ว
**บทเรียน: ทดสอบสวิตช์ต้องปิดทีละตัว การปิดพร้อมกันพิสูจน์อะไรไม่ได้เลย**

### สิ่งที่แก้
| # | แก้ที่ | เนื้อหา |
|---|---|---|
| 1 | `export_exec.py` | คำนวณ `niYE`/`clYE` จาก `trend` ปี 67+68 ของแต่ละแห่งเอง (`YE_FY`) — ไม่ต้องยัดมืออีก |
| 2 | `export_exec.py` | **guard**: `niYE` ต้องมี ≥80% ของแห่ง ไม่งั้น `SystemExit` ก่อนเขียนไฟล์ |
| 3 | `risk_drill.html` | `exYeAdj(mo)` ตัวคูณกันนับซ้ำ — ดูหัวข้อถัดไป |
| 4 | `risk_drill.html` | ป้ายแดงข้างสวิตช์ 📉 เมื่อข้อมูลฤดูกาลไม่มา (ไม่ปล่อยให้เงียบอีก) |
| 5 | `test_exec_backtest.js` | แยกวัด `seas` / `clGrow` ทีละตัว + ตรวจการมีอยู่ของคีย์ตรง ๆ |

### ⭐ `exYeAdj` — กันนับซ้ำเมื่องวดเดินเข้าไตรมาสปิดบัญชีแล้ว
`niYE` คือ **ส่วนต่าง** ของ NI/เดือนช่วงปิดบัญชี เทียบ run-rate ต้นปี แต่ `niM = ni/mo` คิดจาก
ยอดสะสม ซึ่งงวด ด.10 มีเดือนปิดบัญชีจริงปนอยู่แล้ว 1 เดือน — บวก `niYE` เต็มจะนับส่วนนั้นซ้ำ

```
niM = ต้นปี + k·niYE/mo         (k = เดือน ด.≥10 ที่อยู่ในยอดสะสม)
ที่ควรบวก = (ต้นปี + niYE) − niM = niYE · (1 − k/mo)
```

งวด ด.9 ได้ตัวคูณ **1.0 = พฤติกรรมเดิมเป๊ะ** (ไม่กระทบผลที่เคยรายงานไป) · ด.10 = 0.9 · ด.11 = 0.818

### ผลลัพธ์ (งวด 256910 · เป้าระดับ 6)
| | เงินสนับสนุน | ระดับ 0 | ดีขึ้น/แย่ลง |
|---|---|---|---|
| ที่ขึ้นเว็บอยู่ (ฤดูกาลตาย) | **50.85 ลบ. · 12 แห่ง** | 37 | 25 / **5** |
| คืนค่าดิบ ไม่มีตัวคูณ | 83.15 ลบ. · 14 แห่ง | 27 | 12 / 25 |
| **หลังแก้ (มี `exYeAdj`)** | **80.00 ลบ. · 15 แห่ง** | 26 | 12 / **25** |
| อ้างอิง: งวด 256909 | 57.25 ลบ. · 11 แห่ง | 36 | 17 / 29 |

`test_exec_backtest.js` กลับมา **✅ ผ่านทุกข้อ** (เดิมตกข้อ "แย่ลงต้อง ≥7 แห่ง" เพราะได้แค่ 5)
· ชุดตรวจทั้ง 17 ไฟล์เขียวหมด

---

## รอบปรับปรุง 5 ⭐⭐ — ยกเครื่องตาราง "ผลจำลอง" ทั้งตาราง (11–12 ส.ค. 69)

เจ้าของงานสั่งงานต่อเนื่อง 5 รอบในวันเดียว แต่ละรอบ push + ตรวจบน URL จริงก่อนรอบถัดไป
`e3220d5` → `5606f67` → `86211ad` → `f9ee215` → `705ac31` → (รอบยุบ/ขยาย)

### ① เรียงคอลัมน์ใหม่เป็น "สายเลขคณิต" + 6 คอลัมน์เกณฑ์ (`e3220d5`)
14 → 21 คอลัมน์ · อ่านซ้าย→ขวาเป็นสมการ: `เงินสด − เจ้าหนี้(สธ.) + ลูกหนี้(สธ.) = สุทธิ − MOE
→ คงเหลือหลังภาระ MOE → ส่วนขาด → ระดับก่อนช่วย → 💰 6 เกณฑ์ → รวม → ระดับหลังช่วย`
- 🆕 `exNetAfterDebt()` = คอลัมน์ "เงินสด+เทียบเท่า หลังจัดการหนี้สิน" (ตัวกลางที่เคยซ่อนใน `exMoeLeft`)
- 🆕 `exSolveCrit()` = เงินก้อนที่ทำให้ **เกณฑ์รายตัว** ผ่าน (Cash·QR·CR·NWC·NI·SU) · SU วัดที่ "ลด 1 คะแนน"
- ⛔ **ห้ามบวก 6 คอลัมน์** (เงินก้อนเดียวดันทุกเกณฑ์พร้อมกัน) — กันไว้ 4 ชั้น รวมถึงไม่ใส่แถวรวมใน TSV
- 🪤 หัวตาราง 2 ชั้น (colspan) ใช้ไม่ได้เพราะหัวตาราง sticky → ใช้แถบสี `.exneed` + ป้ายเล็กแทน

### ② เดือนที่ใช้ตัดสิน = เดือนที่เลือกในตัวกรอง (`5606f67`)
เดิม `sepRisk` ตรึงที่สิ้นปีงบเสมอ → ครึ่งขวาของตารางไม่ขยับตามตัวกรอง · ตอนนี้ผูก `exMoeMonths()`
ตัวเดียวกับคอลัมน์สภาพคล่อง (Σระดับขยับจริง 1 ด. 268 → 6 ด. 226 → 13 ด. 218)

### ③ ยุบ dropdown "ช่วงจำลอง" รวมกับ "เดือนเป้า" (`86211ad`)
dropdown เดียว โผล่ 2 ที่ (`#exMmoTop`/`#exMmoBot`) ผูก `EXST.mmo` ตัวเดียว · `EX_MMO_MAX` 13→14
- ⚓ **เจ้าของงานเคาะ: คงคุม "ระดับ ณ สิ้นปีงบ" เป็นพื้นเสมอ** → ตัวเลขทุกเดือนเป้า**เท่าเดิมเป๊ะ**
- ⛔ ถ้าตัดออก: เงินหาย 10–21 ลบ. · รพ. หลุด 3–4 แห่ง (เกาะคา/ดอยเต่าเหลือ 0) = โมเดลมองโลกสวยขึ้น

### ④ ทุกช่องตอบ 3 อย่าง: เงิน → ระดับที่จะได้ → กำไร/เดือนทดแทน (`f9ee215`)
- 🆕 **`sc.dni` = กำไรต่อเดือน เป็นตัวแปรใหม่ในแบบจำลอง** (บวกที่ `niM` เพื่อเข้าทั้ง NI และเงินสด)
- 🆕 `exSolveCritNi()` + `exCritLevel()` + ป้าย **✅ พลิก** / **ไม่ขยับ**
- ⭐ `dni` แก้ได้ในเคสที่เงินก้อนแก้ไม่ได้ (NI ข้ามปีงบ) เพราะเข้าทุกเดือน

### ⑤ ป็อปอัปพิสูจน์ที่มาของตัวเลข (`705ac31`)
กดเซลล์ใดก็ได้ใน 6 คอลัมน์ → `exNeedPop()` 4 ส่วน: เกณฑ์+สูตร(รหัสบัญชี) · **สมการย้อนกลับ
(สูตรตรง vs Solver)** · ผลต่อคะแนนทั้ง 7 · **anchor** จากคะแนนเป็นระดับ
- 🐞 ชุดตรวจจับได้ว่าฉบับแรก **เขียนเหตุผลส่วนต่างไว้ล่วงหน้า** ("หนี้สินหมุนเวียนลดลง") ทั้งที่เคสจริง
  ตัวส่วนไม่ขยับ และส่วนต่าง 15K มาจากการปัดขั้น `SV_STEP` → บังคับให้ derive จากตัวเลขทุกครั้ง

### ⑥ ยุบ/ขยายทุกแผงในแท็บ (12 ส.ค. 69)
`exSecWrap()`/`exSecToggle()` — **ค่าเริ่มต้นยุบทุกแผง ยกเว้นตารางผลจำลองที่กางไว้**
สถานะจำใน `EXST.sect` (คนละคีย์กับ `EXST.open` ที่เป็นตารางย่อยข้างในแผง) · แผงสรุปรายจังหวัด
ใช้ `<details>` เดิมอยู่แล้วจึงไม่ห่อซ้ำ · toggle ด้วยการสลับคลาส **ไม่เรียก `exRender`**
(ตารางผลจำลองมี Solver 13 ตัว/แถว ถ้าเรนเดอร์ใหม่ทุกครั้งจะหน่วงเป็นร้อย ms)
⚠️ `@media print` ต้องกางทุกแผง ไม่งั้นบันทึก PDF ได้รายงานที่ขาดตาราง

### 💰 คำถามเจ้าของงานที่ตอบด้วยตัวเลข
- **"ระดับหลังช่วย ≠ ก่อนช่วย ตั้งแต่ยังไม่แก้อะไร"** → คอลัมน์นั้นใส่เงินที่ Solver แนะนำไว้แล้ว
  (88/103 แห่งเท่ากัน = ทุกแห่งที่ไม่ต้องใช้เงิน · 0 เคสผิดปกติ)
- **"เงินที่เติมมากกว่าที่ต้องการ"** → กติกาข้อ ③ ของ ⚡ จัดสรรอัตโนมัติ เติมเกิน 100K/ผู้รับ
- **"เงินก้อนไม่พอ หมายถึงอะไร"** → NI สะสมรีเซ็ต 1 ต.ค. (ลำปาง เติม 300 ลบ. ที่ ก.ย.70 → NI ไม่ขยับเลย)
- **"ใช้เงินน้อยเกินไปไหม"** → ราคาต่อระดับความตั้งใจ: เป้า ≤6 = **79.90 ลบ.** · ≤3 = 327.95 ·
  0 ทุกแห่ง = 1,912.40 · **ผ่านเกณฑ์สภาพคล่องครบ = 1,618.75 ลบ.** · ผ่านทุกเกณฑ์ = 1,926.80 ลบ.
  → 79.90 ลบ. ไม่ได้ต่ำเพราะโมเดลใจดี แต่เพราะเป้าคือ "ไม่เกินระดับ 6" = ลดแค่ 1 คะแนน

ชุดตรวจ **16/16 เขียว** ทุกรอบ · รายละเอียดสูตร/กับดักทั้งหมดอยู่ใน `RISK_EXEC_MODEL.md` หัวข้อ 7.21

---

## รอบปรับปรุง 6 ⭐⭐ — ยกเครื่อง "ตัวขับ" ของแบบจำลองทั้งชุด (12 ส.ค. 69)

เจ้าของงานสั่งแก้ต่อเนื่อง 4 เรื่องในวันเดียว ทุกเรื่องขึ้นเว็บจริงและตรวจซ้ำบน URL แล้ว

### ① NI ต้องไม่ใช่ค่าเฉลี่ยต่อเดือน — `niProf` (`2f496ba`)

โมเดลใช้ **ยอดสะสม ÷ เดือน** เดินหน้าต่อ = ทั้งเขต **+202.7 ลบ./เดือน (กำไร)**
แต่ **ก.ค.69 จริงขาดทุน −299.8 ลบ.** (90/103 แห่งขาดทุน) — **ทิศทางกลับกัน 58/103 แห่ง**

ยืนยันว่าเป็นปัญหา **โมเดล ไม่ใช่ข้อมูล** — ตรวจ 3 ชั้นตรงกันหมด:
`D5317.mdb` ต้นทาง (`DB-HFO\2569-07`) = `master.parquet` **ต่างสูงสุด 0.00 บาท ทั้ง 103 แห่ง**

ของเดิม `niYE` ปรับให้เฉพาะ ด.10-12 ด้วย **ค่าเดียวใช้ทั้ง ก.ค./ส.ค./ก.ย.** → ยอดสะสม ณ ก.ย. ถูก
แต่ **เดือนกลางทางผิด** (สำคัญขึ้นทันทีที่ "เดือนเป้า" เป็นตัวกรองผู้ใช้) และ **ปีงบหน้าไม่ปรับเลย**

โปรไฟล์จริงทั้งเขต: **ต.ค. +866 (ดีสุดของปี)** … **ส.ค. −485 (แย่สุด)** · ผลรวม 12 เดือน = 0

⛔ **ห้ามทำเป็นตัวคูณ** แม้โจทย์ใช้คำว่า "สัดส่วน" — ค่าเฉลี่ยรายเดือนปี 68 = −15.2 ลบ. (ใกล้ 0)
ตัวคูณระเบิดเป็น −58.89 ถึง +44.91 เท่า และฐานติดลบ × ตัวคูณติดลบ = **พลิกขาดทุนเป็นกำไร ผิดทิศ**

**พร้อมกันนั้นแก้บั๊กที่ทำให้ทั้งแท็บดับได้** — `exSimPath` เรียก `scoreOf(...).risk` ตรง ๆ
แต่ `scoreOf` คืน `null` เมื่อ `cl=0` → throw → `exRender()` ตาย → **ทั้งแท็บว่าง ไม่ใช่แถวเดียวหาย**
งวดนี้ยังไม่ระเบิด แต่งวดสด HFO แถวน้อยกว่าปกติ ~22% อยู่แล้ว

### ② NI/NWC เป็น "ระดับ" ไม่ใช่อัตราต่อเดือน (`8bd7016`)

เกณฑ์ทางการวัดที่ **ระดับ**: `NI สะสมทั้งปีงบ ≥ 0` · `NWC ณ จุดเวลา ≥ 0`
แต่ตารางโชว์ `NI /เดือน` = **อัตราที่ใช้เดินแบบจำลอง ไม่ใช่ตัวที่เกณฑ์ตัดสิน** (เห็นเลขบวกแล้ว
เข้าใจว่าผ่าน ทั้งที่ NI สะสมอาจติดลบ) และ **NWC ไม่มีคอลัมน์เลย** ทั้งที่เป็น 1 ใน 2 เกณฑ์ ST

→ `NI สะสม ณ <เดือนเป้า>` + คอลัมน์ `NWC ณ <เดือนเป้า>` ใหม่ · ตัวใหญ่ = **ค่าเดียวกับที่ Solver
ใช้ตัดสินเป๊ะ** · บรรทัดเล็ก = ค่าปัจจุบัน + ดีขึ้น/แย่ลง · ขยับตามตัวกรองเดือนเป้าเอง

🪤 NI สะสมรีเซ็ต 1 ต.ค. → เดือนเป้าข้ามปีงบขึ้นป้าย **"เริ่มนับใหม่ปีงบหน้า"**
(ลี้ −3.04M ณ ก.ค.69 → +7.30M ณ ม.ค.70 ไม่ใช่เพราะฟื้น)

### ③ ⭐ เติมบรรทัดที่หายไปของงบกระแสเงินสด (`2713340`)

เจ้าของงานบอกว่าคำอธิบายเดิม **ไม่ใช่ภาษาบัญชี** จึงยังไม่เข้าใจ → อธิบายด้วยกรอบจริง:

```
  กำไรสุทธิ
+ รายการที่ไม่ใช่เงินสด (ค่าเสื่อมราคา/ตัดจำหน่าย/หนี้สงสัยจะสูญ − รายได้รับบริจาคสินทรัพย์)
± การเปลี่ยนแปลงในสินทรัพย์และหนี้สินดำเนินงาน        ← ⛔ ของเดิม "ไม่มีบรรทัดนี้เลย"
= เงินสดสุทธิจากกิจกรรมดำเนินงาน
```

ของเดิมบวกกำไรทั้งก้อนเข้า CA/สินทรัพย์เร็ว/เงินสด **เท่ากันหมด** = สมมติเก็บเป็นเงินสดทันที 100%
ทั้งที่ **ลูกหนี้ + สินค้าคงเหลือ = 54% ของสินทรัพย์หมุนเวียน** (5,853 + 1,542 จาก 13,598 ลบ.)
ผลคือ **ระยะห่าง CR–QR–Cash ไม่มีวันเปลี่ยน** ตลอดการจำลอง

เพิ่ม `arProf`/`invProf` เป็น **การจัดประเภทใหม่ภายใน CA** (ยอดรวมไม่เปลี่ยน) + `clProf`

| | ก่อน | +clProf | **+เงินทุนหมุนเวียน** | ของจริง |
|---|---|---|---|---|
| ทำนาย "แย่ลง ณ ก.ย." | 7 | 6 ❌ | **10** ✅ | ปี67 33 · ปี68 65 |
| ระดับ 0 | 40 | 43 | **35** | ปี67 17 · ปี68 11 |
| เงินสนับสนุนเป้า ≤6 | 104.35 | 104.95 | **104.20 ลบ./18 แห่ง** | — |

**ยอดรวมลดเล็กน้อยแต่ backtest แย่ลงมากขึ้น = โมเดลสมจริงขึ้น ไม่ใช่ผ่อนเกณฑ์**
ช่องว่าง CR−QR เปลี่ยนได้แล้ว **103/103 แห่ง**

🪤 **ตัวหักกันนับซ้ำใช้ช่วงเดือนไม่เท่ากัน**: `niM` มาจากยอดสะสม ÷ mo = ด.**1..mo**
แต่ `clMo`/`arProf`/`invProf` วัดจาก **ผลต่างของยอดคงเหลือ** = ด.**2..mo**
(ใช้ช่วงผิดแล้ว "แย่ลง" ร่วงจาก 7 เหลือ 5 แห่ง)

### ④ 📈 ป็อปอัป "ทำไมระดับถึงเปลี่ยน" (`8fe2fc4`)

คลิก badge **ระดับก่อนช่วย** → สะพานคะแนน: คะแนนงวดปัจจุบัน → ก่อนช่วย ณ เดือนเป้า → หลังเติมเงิน
① ปัจจัยระหว่างทาง (**ปิดทีละตัวแล้ววัดส่วนต่างจริง** ไม่ใช่เดา) ② มาตรการที่เลือก รวม Option ยกหนี้
พร้อมระดับ "ถ้าไม่ทำ" ③ เงินเติมให้พอ MOE ④ ผลลัพธ์รายเกณฑ์ + เกณฑ์ที่พลิก

⚠️ ③ ย้ำบนจอว่า **เงินเติมให้พอ MOE ≠ เงินสนับสนุนที่เล็งระดับวิกฤต** (เกณฑ์คะแนนวัดอัตราส่วน
งบดุล ไม่ได้วัดว่าเงินพอจ่ายหรือไม่) = จุดที่เข้าใจผิดบ่อยที่สุด

⚠️ ป็อปอัปสลับ `EXST` ชั่วคราวเพื่อจำลอง "ถ้าปิดปัจจัย" → **ต้องคืนค่าใน `finally` เสมอ**
ไม่งั้นทั้งหน้าค้างเป็น `seas:false`/`clGrow:false` แล้วตัวเลขทั้งตารางเพี้ยนตาม

---

## รอบปรับปรุง 7 🏦 — เงินของเขตที่ฝากไว้กับ รพ. (12 ส.ค. 69 · เจ้าของงานแจ้ง)

เขตฝากเงินไว้กับ **นครพิงค์ 47.4** (เดิม 57.4 − โยกให้ สนง.เขตฯ 10.0) · **ลำปาง 57.4** ·
**เชียงรายประชานุเคราะห์ 57.4** ลบ. รวม **162.2 ลบ.** — **ไม่ใช่เงินของ รพ. เอง**
ต้องหักออกจากเงินสด+เทียบเท่า **ตั้งแต่ต้น** ไม่งั้นการคำนวณเพี้ยน

⚠️ ตรวจก่อนหัก: งบ 256910 **ไม่มีบัญชีเงินรับฝากคู่กันฝั่งหนี้สิน** (เงินนอนอยู่ใน
`1101030102.101` เงินฝากธนาคาร-นอกงบประมาณ เฉย ๆ) ⇒ หักฝั่งเงินสดได้โดยไม่นับซ้ำ

⛔ หักฝั่งเงินสดอย่างเดียว ห้ามแตะหนี้สิน · ⛔ ห้ามรวมเข้า `inj` (inj เข้า NI — การหักเงินฝาก
ไม่ใช่ค่าใช้จ่าย) · ยอดอยู่ที่ `REGION_DEPOSIT` ใน `export_exec.py` ที่เดียว ห้าม hardcode ซ้ำ

**ผล**: นครพิงค์ 0→1 · ลำปาง 1→2 (ณ ก.ย.69) · เงินสนับสนุนเป้า ≤6 **104.20 → 107.65 ลบ.**

**กลไกใช้เติม**: แผง 🏦 ใหม่ เลือกได้ว่าดึงจากก้อนใคร → เติมให้ใคร → เท่าไหร่
+ ⚡ จัดสรรอัตโนมัติ (ไล่ปิดส่วนขาดสภาพคล่อง MOE) · งวดนี้ 23 รายการ 87.05 ลบ. เหลือ 75.15 ลบ.
⚠️ ไม่เติมให้ผู้ถือเงินฝากเอง — เงินอยู่ในมือเขาแล้ว

**🐞 แก้บั๊กพร้อมกัน: QR > CR** (เป็นไปไม่ได้ — สินทรัพย์เร็ว ⊆ สินทรัพย์หมุนเวียน)
บรรทัดเงินทุนหมุนเวียนมีเพดานแค่ขาขึ้น ขาลงปล่อยสินค้าคงเหลือคืนเกินยอดที่มีจริง
(ฮอด QR 1.142 > CR 1.075) → ใส่เพดาน 2 ทาง

**ชุดตรวจ 20/20 เขียว** · คู่มือ `RISK_EXEC_MODEL.md` **7.27**

---

## ✅ ปิดประเด็นแล้ว — บัญชีทุนหมวด 3 ไม่ต้องแตะ

เจ้าของงานยืนยัน 12 ส.ค. 69: **หมวด 3 (ทุน) ไม่ได้นำมาใช้อะไรในทางบัญชีของ รพ. จึงไม่แตะ**
→ ประเด็น "งบดุลไม่ articulate 983 ลบ." ด้านล่าง **ไม่ต้องไปไล่ให้ดุลผ่านฝั่งทุน**
เก็บบันทึกไว้เป็นข้อมูลว่าโมเดลปรับเชิงประจักษ์ตรงไหนบ้าง

**งบดุลของโมเดลยังไม่ articulate 983 ลบ.** (2 เดือน ทั้งเขต) — แยกที่มาได้เป๊ะ:
- `niProf` (ปรับ NI ตามฤดูกาลปิดบัญชี) กระทบ **ทุน** แต่ไม่มีคู่บัญชี → **−1,116 ลบ.**
- `clMo`+`clProf` (หนี้สินโตตาม run-rate) กระทบ **หนี้สิน** แต่ไม่มีคู่บัญชี → **+133 ลบ.**

ทั้งคู่เป็นการปรับ **เชิงประจักษ์** ไม่ใช่การลงคู่บัญชี ผลจริงคือรายการปิดบัญชีปลายปี
(ตั้งค่าใช้จ่ายค้างจ่าย · ตัดลูกหนี้ · ค่าเสื่อมงวดท้าย) **กด NI ลง แต่ CR/QR/Cash/NWC ไม่รู้สึก**

ต้องเคาะก่อนว่าจะแบ่งผลกระทบฝั่งงบดุลอย่างไร — เข้าหนี้สินหมุนเวียนเท่าไหร่ · ตัดลูกหนี้เท่าไหร่ ·
เข้าสินทรัพย์ถาวรเท่าไหร่ (ค่าเสื่อม ซึ่งไม่กระทบ CA/CL)

**ชุดตรวจ 19/19 เขียว** ทุกรอบ (เพิ่มใหม่ 4 ชุด: `test_exec_niprof` · `test_exec_why` ·
`pw_test_niprof` · `pw_test_nilevel` · `pw_test_wc_why`)
รายละเอียดสูตร/กับดักทั้งหมดอยู่ใน `RISK_EXEC_MODEL.md` หัวข้อ **7.22–7.26**
